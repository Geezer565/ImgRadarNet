# ImgRadarNet

融合图像和雷达数据的神经网络。

这是智能车项目的最终代码纪念版，保留模型结构、数据读取、训练和推理脚本；原始数据、训练权重和运行输出均未上传。

主要文件：

- `model.py`：图像与雷达融合网络。
- `dataset.py`：训练数据读取。
- `train.py`：训练入口。
- `infer.py`、`test.py`：推理和验证。

本仓库只保存一份最终快照，不保留中间版本和自动生成文件。

